# Privox Monitor — план действий (демо-сначала)

> Создано 2026-06-18. Конкретный пошаговый план «что делаем руками».
> Опирается на [ARCHITECTURE_PLAN_RU.md](ARCHITECTURE_PLAN_RU.md) (как строим) и
> [PRIVOX_MONITOR_RU.md](PRIVOX_MONITOR_RU.md) (продукт/вертикали).
>
> **Решения 2026-06-18 (Eugene):**
> 1. **Демо-сначала** — поднимаем `monitor.privox.tech` поверх ТЕКУЩЕГО бэкенда; `tenant_id` откладываем до 2-го клиента.
> 2. **Отдельная папка+репо** — `~/IT PROJECT/PrivoxMonitor` (свой git, свой деплой, изолирован от автодеплоя PrivoxPTT).
> 3. **СНАЧАЛА САЙТ, ПОТОМ УВЕДОМЛЕНИЯ (пересмотр вечером 2026-06-18).** Логика продукта: сначала клиент должен
>    ВИДЕТЬ датчики на сайте и задать критерии нормальной работы (пороги), и только при нарушении уходит
>    оповещение. Поэтому порядок: фронт-витрина → выбор датчиков + критерии → деплой → **Telegram в самом конце**.
>    (Telegram-код уже написан и проверен, но выключен no-op'ом — ждёт очереди. Раньше ошибочно начали с него.)

---

## Что НЕ делаем сейчас (осознанно отложено)
- ❌ `tenant_id` / мультиарендность / роль OWNER — нужны со 2-го клиента. Сейчас 1 демо-tenant = org `privox`.
- ❌ Биллинг, самостоятельная регистрация — фаза 3.
- ❌ Пресеты по вертикалям (ферма/cold chain) — фаза 2, после рабочего MVP.
- ✅ Бэкенд датчиков (телеметрия/правила/инциденты/пуши) — **уже на проде, не трогаем**.
- ⏸️ Telegram-оповещения — код готов и проверен, но это **последняя фаза**, после рабочей витрины с критериями.

---

## Архитектура демо
```
   [7 наших датчиков] ──> POST /api/telemetry ──> ОБЩИЙ БЭКЕНД (ptt.privox.tech, как есть)
                                                      │  правила/инциденты/FCM-пуши ✅
                                                      │  + Telegram-бот (НОВОЕ, в server/)
                                                      ▼
                           monitor.privox.tech  ──> тот же API (датчиковые эндпоинты)
                           НОВЫЙ фронт PrivoxMonitor (свой репо, бренд, без PTT)
```

---

## Фазы

> ⚠️ ПОРЯДОК ПЕРЕСМОТРЕН 2026-06-18: сначала САЙТ-витрина с критериями, оповещения (Telegram) — в самом конце.

### Фаза 0 — ревизия того, что уже отдаёт бэкенд (на мне, до кода фронта)
- [ ] Какие датчиковые эндпоинты уже есть: `GET /api/sensors` (список+метрики+статус), инциденты, sensor-update/sensor-alert по сокету, редактирование порогов (`routes/sensors.ts`).
- [ ] Чего не хватает фронту Monitor для «выбрать датчик + задать критерии нормы» — список доработок бэка (если нужны).

### Фаза 1 — каркас сайта PrivoxMonitor (отдельный репо) ← СТАРТ
- [ ] Создать `~/IT PROJECT/PrivoxMonitor`, `git init`, каркас **Vite + React + TS** (как `PrivoxPTT/web`).
- [ ] Скопировать/адаптировать API-клиент и типы датчиков из `PrivoxPTT/web` (общий npm-пакет — позже).
- [ ] Брендинг **Privox Monitor**: лого, палитра, **ни одного упоминания PTT/раций**.
- [ ] Логин: на демо переиспользуем существующий JWT-логин бэка. Роль OWNER + scope — позже.
- [ ] Подключение к прод-API (`https://ptt.privox.tech/api`) через env базового URL.

### Фаза 2 — дашборд: ВИДЕТЬ всё на мониторе (ядро продукта)
- [ ] **Дашборд датчиков** — список с OK/ALERT/STALE, последнее значение, батарея/связь, время.
- [ ] **Карточка датчика** — текущие метрики + история/график.
- [ ] **Лента инцидентов**.
- [ ] Живые обновления по сокету (sensor-update/sensor-alert).

### Фаза 3 — критерии нормальной работы (пороги из UI)
- [ ] Экран «выбрать датчик → задать критерии нормы»: для каждой метрики правило (диапазон/порог + severity).
  Бэкенд это уже умеет (ThresholdRule: op gt/lt/outside/is + severity) — Monitor даёт удобный UI поверх.
- [ ] Вкл/выкл наблюдения и (позже) канала оповещения на датчик.
- [ ] Проверка: нарушил порог → инцидент виден на сайте (ALERT), вернулось в норму → отбой.

### Фаза 4 — деплой monitor.privox.tech
- [ ] **Eugene/я:** новый сервис `monitor-frontend` в Coolify, источник = репо PrivoxMonitor.
- [ ] **Eugene:** DNS `monitor.privox.tech` → сервис фронта (бэкенд остаётся `ptt.privox.tech` для API).
- [ ] CORS на бэке: разрешить origin `monitor.privox.tech`.
- [ ] Проверка end-to-end: заходим на monitor.privox.tech → видим 7 датчиков → нарушение порога → ALERT на сайте.

### Фаза 5 — ОПОВЕЩЕНИЯ при нарушении (Telegram) ← в самом конце
*Код Telegram уже написан и проверен (`server/services/telegram.ts` + хук в `process.ts`), выключен no-op'ом.*
- [ ] Включить на проде: `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` в Coolify env, выкатить бэк.
- [ ] (Позже) выбор канала/получателя на датчик из UI Monitor; привязка chat_id к клиенту.
- [ ] (Опц.) Telegram и на ОТБОЙ (resolve), не только на аларм.

### Фаза 6 — после демо (по мере появления клиентов)
- [ ] `tenant_id` на Sensor/Incident/User + scope-middleware + миграция дефолтного tenant (фундамент мультиарендности).
- [ ] Роль **OWNER**, ручной завод аккаунтов клиентов.
- [ ] Пресеты по вертикалям (ферма, cold chain, парковки).
- [ ] Биллинг / самостоятельная регистрация.

---

## Открытые вопросы на потом (не блокируют старт)
- Логин Monitor на проде: оставить общий JWT или сразу отдельные OWNER-аккаунты? (для демо — общий, решим при 2-м клиенте).
- Общий npm-пакет типов/API между `web` и PrivoxMonitor — причешем, когда фронт стабилизируется.
- Графики истории: нужен ли отдельный time-series или хватит текущих `SensorReading` из БД.

---

## Память / связанные
Решения зафиксированы в `project_privox_monitor` (память). Связано: реестр датчиков
`privox-sensors/SENSORS_INVENTORY_RU.md`, бэкенд B8 на проде ([[project_b8_deployed]]).
</content>
